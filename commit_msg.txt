feat(all): add Gemini provider, scoped logging, and streaming artifacts

MAJOR RELEASE:

1. New Gemini Provider:
   - Added `GeminiLlmService` using Google.GenAI SDK.
   - Implemented `GeminiCacheManager` for Context Caching.
   - Implemented `GeminiFileManager` for native file API support.
   - Added support for "Thinking" budget and Google Search grounding.

2. Observability & Logging:
   - Implemented automatic Scoped Logging in Pipeline (`Step`, `Path`, `CorrelationId`).
   - Added `TagStartedEvent` and `TagCompletedEvent` for streaming artifacts.
   - Standardized artifact events as "fire-and-forget" side-effects.

3. Core Enhancements:
   - Added `McpToolWrapper` for Model Context Protocol integration.
   - Updated Middleware loggers to use typed `ILogger<T>`.

4. Documentation:
   - Complete taxonomy refactor in `Funcional.md` (ES/EN).
   - Added Scoped Logging and Event System guides.
